# ADR 0001：Architecture Wiki 只激活一种语言

**状态：** 已采纳

## 决策

Release 自包含中文和英文两个未激活资产。安装器根据显式参数把其中一个复制到 Pi Package 的同一 Skill 位置，并把语言写入 Workstation 状态；更新和修复沿用该值，显式切换时原子重建 Package。

## 取舍

同时安装两个变体能省去状态，但会让同一能力出现两个入口，增加 Agent 路由歧义。只发行当前语言会让切换依赖网络或维护源。双资产、单激活多占一份发行体积，换来离线切换和唯一运行状态。
